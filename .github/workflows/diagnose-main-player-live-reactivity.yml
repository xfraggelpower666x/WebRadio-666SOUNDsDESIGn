name: Diagnose Main Player Live Reactivity

on:
  pull_request:
    branches:
      - WebRadio-666SOUNDsDESIGn

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: repair-main-player-reactivity-metadata-20260723
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Reconstruct and apply exact repair payload
        shell: python
        run: |
          from pathlib import Path
          workflow = Path('.github/workflows/finalize-main-player-live-reactivity.yml').read_text(encoding='utf-8')
          start_marker = '      - name: Prepare canonical repair payload\n        shell: python\n        run: |\n'
          end_marker = '      - name: Repair canonical visual and metadata ownership\n'
          start = workflow.index(start_marker) + len(start_marker)
          end = workflow.index(end_marker, start)
          lines = workflow[start:end].splitlines()
          code = '\n'.join(line[10:] if line.startswith('          ') else line for line in lines)
          exec(compile(code, 'prepare-main-reactive.py', 'exec'), {'__name__':'__main__'})
          exec(compile(Path('/tmp/transform-main-reactive.py').read_text(encoding='utf-8'), 'transform-main-reactive.py', 'exec'), {'__name__':'__main__'})
      - name: Capture exact targeted test output
        shell: bash
        run: |
          set +e
          node --test tests/main-player-reactive-visual-contract.test.mjs > /tmp/main-reactive-test.log 2>&1
          echo $? > /tmp/main-reactive-exit.txt
          exit 0
      - uses: actions/upload-artifact@v4
        with:
          name: main-reactive-contract-diagnostic
          path: |
            /tmp/main-reactive-test.log
            /tmp/main-reactive-exit.txt
