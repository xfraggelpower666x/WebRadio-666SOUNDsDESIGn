name: Repair Discord Access Denied Diagnostics
on:
  pull_request:
    branches: [WebRadio-666SOUNDsDESIGn]
    types: [opened, synchronize, reopened]
permissions:
  contents: write
jobs:
  repair:
    if: github.head_ref == 'repair/discord-access-denied-diagnostics-20260805'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: repair/discord-access-denied-diagnostics-20260805
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Apply isolated repair
        run: node scripts/repair-discord-access-denied-diagnostics.mjs
      - name: Run regression tests
        run: node --test tests/discord-access-denied-diagnostics.test.mjs tests/discord-central-error-time-reset.test.mjs tests/discord-private-retry-error-time.test.mjs
      - name: Self-clean and commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm scripts/repair-discord-access-denied-diagnostics.mjs .github/workflows/repair-discord-access-denied-diagnostics.yml
          git add worker-addons/discord-notify-addon-v3.js workers/webradio-666soundsdesign-worker/worker-addons/discord-notify-addon-v3.js tests/discord-access-denied-diagnostics.test.mjs
          git commit -m "Record Discord access-denied diagnostics"
          git push
