666SOUNDsDESIGn — STRICT MOBILE FRONTEND PURGE v6

Zweck:
- Die alte untere cyan/magenta Levelmeter-Leiste wurde von js/player-core.js nachträglich erzeugt.
- Diese alte DOM-Meter-/Transport-Reparatur ist jetzt in js/player-core.js deaktiviert.
- Damit kann der alte Bottom-Balken nicht mehr nachgeneriert werden.
- Finaler #mffApp bleibt die einzige sichtbare Mobile-UI.
- Metadaten werden nur noch über echte API/JSON-Routen versucht:
  /api/nowplaying
  /nowplaying
  /meta
  /status
- HTML/DOCTYPE-Antworten werden strikt verworfen und nicht mehr als Tracktitel angezeigt.

Geändert:
- index.html
- css/mobile.css
- js/player-core.js
- CHANGELOG_STRICT_MOBILE_FRONTEND_PURGE_v6.txt

Nicht geändert:
- worker.js
- wrangler.jsonc
- Stream-Routen
- Root-Migration
- Boost-Multipliers
