ASSET FIX BUILD

Goal:
- Keep stream / metadata / health routes intact.
- Keep player separated in repo files.
- Fix current white-page issue by explicitly serving the separated files from worker routes.

This build does NOT change player design or features.
It only repairs delivery of:
- /
- /css/main.css
- /js/app.js
- /config/stream.config.js
