# RENDERER RESOURCE INTEGRATED — 2026-05-24

Renderer deploy-awareness repo is integrated as a separate resource package.

```text
renderer-resources/
└── 666SOUNDsDESIGn-Alert-Service-Renderer/
```

This does not update the separate Renderer GitHub repo automatically unless your upload/deploy tool handles this folder. For the live Renderer repo, upload this folder's contents to the Renderer repo.

Player root is preserved.
