# v190 Mobile Graphic EQ Overlay

- Tap `#mffEqBars` or `#eqBars` to open POWER EQ overlay.
- Uses existing hidden PC real EQ inputs/state (`smfp_real_eq_v162`).
- No SEND/Worker/Backend/Discord changes.
- Hidden overlay uses pointer-events only while open.
- Settings persist via existing EQ localStorage/state.
