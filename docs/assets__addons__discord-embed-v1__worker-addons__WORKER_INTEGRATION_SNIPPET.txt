############################################################
# 666SOUNDsDESIGn — Worker Integration Snippet
# Created: 2026-05-05
# Modified: 2026-05-05
# Version: V1.0
# Purpose: Minimal add-only patch points for existing worker.js.
############################################################

A) GANZ OBEN IN worker.js EINFÜGEN:

import { handleDiscordPlayerCard } from './worker-addons/discord-embed-addon.js';

B) IN export default { async fetch(request, env, ctx) { ... } }
   DIREKT NACH const url = new URL(request.url); EINFÜGEN:

if (url.pathname === '/api/discord/player-card') {
  return handleDiscordPlayerCard(request, env);
}

C) CLOUDFLARE SECRET SETZEN:

DISCORD_WEBHOOK_URL = deine Discord Webhook URL

Optional:
DISCORD_ADMIN_TOKEN = eigenes Admin-Token

D) TEST:

Browser:
https://webradio.666soundsdesign-broadcaster.com/api/discord/player-card

Erwartet: JSON mit ok:true und Route-Info.
