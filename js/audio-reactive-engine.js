(()=>{const audio=()=>document.getElementById("audioPlayer");let started=false;function fit(canvas,ctx){if(!canvas||!ctx)return;const rect=canvas.getBoundingClientRect(),dpr=Math.min(window.devicePixelRatio||1,2),w=Math.max(1,Math.round(rect.width*dpr)),h=Math.max(1,Math.round(rect.height*dpr));if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h;ctx.setTransform(1,0,0,1,0,0);ctx.scale(dpr,dpr)}}function buildChain(){const a=audio();if(!a)return false;const Ctx=window.AudioContext||window.webkitAudioContext;if(!Ctx)return false;if(!window._audioCtx){const ctx=new Ctx(),src=ctx.createMediaElementSource(a),eq=ctx.createBiquadFilter(),masterGain=ctx.createGain(),analyser=ctx.createAnalyser();eq.type="lowshelf";eq.frequency.value=200;eq.gain.value=0;analyser.fftSize=128;src.connect(eq);eq.connect(masterGain);masterGain.connect(analyser);analyser.connect(ctx.destination);window._audioCtx=ctx;window._sourceNode=src;window._eqNode=eq;window._masterGainNode=masterGain;window._analyser=analyser}return true}function drawMeters(avg){["meterLCanvas","meterRCanvas"].forEach(id=>{const canvas=document.getElementById(id);if(!canvas)return;const ctx=canvas.getContext("2d");fit(canvas,ctx);const rect=canvas.getBoundingClientRect(),w=rect.width,h=rect.height,bars=DeviceMode.mode==="iphone"?12:18,gap=4,barH=(h-gap*(bars-1))/bars,active=Math.max(1,Math.round(avg*bars));ctx.clearRect(0,0,w,h);for(let i=0;i<bars;i++){const y=h-(i+1)*barH-i*gap;ctx.globalAlpha=i<active?.95:.12;const grad=ctx.createLinearGradient(0,y,w,y+barH);grad.addColorStop(0,"#00e5ff");grad.addColorStop(1,"#ff2fd1");ctx.fillStyle=grad;ctx.fillRect(0,y,w,barH)}ctx.globalAlpha=1})}function drawEq(data){const canvas=document.getElementById("eqCanvas");if(!canvas)return;const ctx=canvas.getContext("2d");fit(canvas,ctx);const rect=canvas.getBoundingClientRect(),w=rect.width,h=rect.height,bars=DeviceMode.mode==="iphone"?28:40,barW=w/bars*.68,gap=(w-bars*barW)/(bars+1);ctx.clearRect(0,0,w,h);for(let i=0;i<bars;i++){const v=(data[i]||0)/255,bh=Math.max(8,v*h*.9),x=gap+i*(barW+gap),y=h-bh,grad=ctx.createLinearGradient(0,y,0,h);grad.addColorStop(0,"#55e8ff");grad.addColorStop(1,"#ff2fd1");ctx.fillStyle=grad;ctx.globalAlpha=.9;ctx.fillRect(x,y,barW,bh)}ctx.globalAlpha=1}function frame(){const analyser=window._analyser;if(!analyser){requestAnimationFrame(frame);return}const data=new Uint8Array(analyser.frequencyBinCount);analyser.getByteFrequencyData(data);let sum=0;for(let i=0;i<data.length;i++)sum+=data[i];const avg=(sum/data.length)/255;drawMeters(avg);drawEq(data);if(avg>.35)document.body.classList.add("beat");else document.body.classList.remove("beat");if(avg>.58)document.body.classList.add("drop");else document.body.classList.remove("drop");requestAnimationFrame(frame)}window.AudioReactiveEngine={async start(){if(!buildChain())return false;if(window._audioCtx&&window._audioCtx.state==="suspended")await window._audioCtx.resume();if(window.AutoBoostPro&&!window.__autoBoostStarted){window.__autoBoostStarted=true;window.AutoBoostPro.start()}if(!started){started=true;frame()}return true}}})();