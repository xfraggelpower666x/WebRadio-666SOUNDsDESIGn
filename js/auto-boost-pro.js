window.AutoBoostPro={target:.72,attack:.02,release:.005,maxGain:1.6,minGain:.6,currentGain:1,enabled:true,getAnalyser(){return window._analyser||null},getGainNode(){return window._gainNode||null},computeLevel(d){let s=0;for(let i=0;i<d.length;i++)s+=d[i];return s/d.length/255},process(avg){const diff=this.target-avg;if(diff>0)this.currentGain+=diff*this.attack;else this.currentGain+=diff*this.release;this.currentGain=Math.max(this.minGain,Math.min(this.maxGain,this.currentGain))},apply(){const g=this.getGainNode();if(g)g.gain.value=this.currentGain},loop(){if(!this.enabled){requestAnimationFrame(()=>this.loop());return}const a=this.getAnalyser(),g=this.getGainNode();if(!a||!g){requestAnimationFrame(()=>this.loop());return}const d=new Uint8Array(a.frequencyBinCount);a.getByteFrequencyData(d);this.process(this.computeLevel(d));this.apply();requestAnimationFrame(()=>this.loop())},start(){this.enabled=true;this.loop()},stop(){this.enabled=false}};