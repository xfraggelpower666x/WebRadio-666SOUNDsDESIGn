{
  "name": "webradio-666soundsdesign-fullversion",
  "version": "1.2.16",
  "private": true,
  "type": "module",
  "description": "666SOUNDsDESIGn WebRadio full source with central animated splash across all players, fixed iPhone geometry, smart proportional iPhone footer banner, and preserved EQ-then-Boost limiter audio chain.",
  "scripts": {
    "check": "node scripts/check-release.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm test",
    "deploy": "npx wrangler deploy",
    "deploy:legacy-root": "cd workers/webradio-666soundsdesign-worker && npx wrangler deploy",
    "verify:deployment": "node scripts/verify-deployment.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
