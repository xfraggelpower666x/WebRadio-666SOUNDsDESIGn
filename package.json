{
  "name": "webradio-666soundsdesign-fullversion",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "description": "666SOUNDsDESIGn WebRadio full source, Cloudflare Worker, Scriptable uploader and separated backend resources.",
  "scripts": {
    "check": "node scripts/check-release.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
