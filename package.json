{
  "name": "webradio-666soundsdesign-fullversion",
  "version": "1.2.3",
  "private": true,
  "type": "module",
  "description": "666SOUNDsDESIGn WebRadio full source with preserved player architecture, hard-routed AMARIS LYVRA recovery player, iPhone no-scroll viewport layout, compact PC mini-card, centralized title de-duplication and canonical LYVRA DJ/live-DJ switching.",
  "scripts": {
    "check": "node scripts/check-release.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm test",
    "deploy": "npx wrangler deploy",
    "deploy:legacy-root": "cd workers/webradio-666soundsdesign-worker && npx wrangler deploy",
    "verify:deployment": "node scripts/verify-deployment.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
