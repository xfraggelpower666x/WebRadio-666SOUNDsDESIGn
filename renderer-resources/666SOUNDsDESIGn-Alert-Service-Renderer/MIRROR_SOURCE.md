# Renderer Mirror

Kompatibilitäts-Spiegel der kanonischen Quelle unter:

```text
Render/666SOUNDsDESIGn-Alert-Service-Renderer/
```

Die Laufzeitdateien `src/server.py`, `requirements.txt`, `render.yaml` und `.env.example` müssen bytegleich bleiben. Das Release-Gate prüft diese Übereinstimmung.
