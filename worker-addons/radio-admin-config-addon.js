/*
FILE: worker-addons/radio-admin-config-addon.js
CREATED: 2026-05-21
PURPOSE: Protected GitHub-backed radio runtime config API.
*/
const DEFAULT_CONFIG_PATH = "config/radio-runtime.json";
const DEFAULT_BACKUP_DIR = "config/backups";
function adminJson(data,status=200){return new Response(JSON.stringify(data,null,2),{status,headers:{"content-type":"application/json; charset=UTF-8","cache-control":"no-store"}})}
function getCookie(request,name){const raw=request.headers.get("cookie")||"";for(const part of raw.split(";").map(v=>v.trim())){const eq=part.indexOf("=");if(eq>-1&&part.slice(0,eq)===name)return decodeURIComponent(part.slice(eq+1));}return""}
async function verifyAdminAuth(request,env){const verifyUrl=env.ADMIN_AUTH_VERIFY_URL||"https://666-system-auth.666soundsdesign-broadcaster.com/verify";const token=getCookie(request,"chaos_auth");const headers=token?{authorization:`Bearer ${token}`}:{};
try{const res=await fetch(verifyUrl,{headers});if(!res.ok)return{ok:false,status:res.status};return res.json();}catch(err){return{ok:false,error:String(err&&err.message?err.message:err)}}}
function requiredEnv(env){return["GITHUB_TOKEN","GITHUB_OWNER","GITHUB_REPO","GITHUB_BRANCH"].filter(k=>!env[k])}
function ghHeaders(env){return{"authorization":`Bearer ${env.GITHUB_TOKEN}`,"accept":"application/vnd.github+json","content-type":"application/json","user-agent":"666-radio-admin-config-addon"}}
function ghApi(env,path){return`https://api.github.com/repos/${env.GITHUB_OWNER}/${env.GITHUB_REPO}/contents/${path}`}
function b64enc(str){const bytes=new TextEncoder().encode(str);let bin="";for(const b of bytes)bin+=String.fromCharCode(b);return btoa(bin)}
function b64dec(str){const bin=atob(String(str||"").replace(/\n/g,""));return new TextDecoder("utf-8").decode(Uint8Array.from(bin,c=>c.charCodeAt(0)))}
async function githubGetFile(env,path){const res=await fetch(`${ghApi(env,path)}?ref=${encodeURIComponent(env.GITHUB_BRANCH)}`,{headers:ghHeaders(env)});const data=await res.json();if(!res.ok)return{ok:false,status:res.status,error:data};return{ok:true,sha:data.sha,content:b64dec(data.content||""),path:data.path}}
async function githubPutFile(env,path,content,message,sha){const body={message,content:b64enc(content),branch:env.GITHUB_BRANCH};if(sha)body.sha=sha;const res=await fetch(ghApi(env,path),{method:"PUT",headers:ghHeaders(env),body:JSON.stringify(body)});const data=await res.json();if(!res.ok)return{ok:false,status:res.status,error:data};return{ok:true,data}}
async function githubListDir(env,path){const res=await fetch(`${ghApi(env,path)}?ref=${encodeURIComponent(env.GITHUB_BRANCH)}`,{headers:ghHeaders(env)});const data=await res.json();if(!res.ok)return{ok:false,status:res.status,error:data};return{ok:true,items:Array.isArray(data)?data.map(x=>({name:x.name,path:x.path,sha:x.sha,size:x.size,type:x.type})):[]}}
function stamp(){return new Date().toISOString().replace(/[:.]/g,"-")}
function validatePayload(body){const errors=[];for(const [key,label] of [["primaryStream","Primary Stream"],["backupStream","Backup Stream"],["emergencyStream","Emergency Stream"]]){const value=String(body[key]||"").trim();if(!value)continue;try{const u=new URL(value);if(!/^https?:$/.test(u.protocol))errors.push(`${label}: only http/https allowed`)}catch{errors.push(`${label}: invalid URL`)}}return errors}
async function requireAdmin(request,env){const auth=await verifyAdminAuth(request,env);if(!auth.ok)return{response:adminJson({ok:false,error:"unauthorized",auth},401)};const missing=requiredEnv(env);if(missing.length)return{response:adminJson({ok:false,error:"missing_env",missing},503)};return{auth}}
async function current(request,env){const gate=await requireAdmin(request,env);if(gate.response)return gate.response;const path=env.GITHUB_CONFIG_PATH||DEFAULT_CONFIG_PATH;const cur=await githubGetFile(env,path);if(!cur.ok)return adminJson({ok:false,error:"github_read_failed",current:cur},502);let config;try{config=JSON.parse(cur.content)}catch{config={raw:cur.content}}return adminJson({ok:true,path,sha:cur.sha,config})}
async function backups(request,env){const gate=await requireAdmin(request,env);if(gate.response)return gate.response;const dir=env.GITHUB_BACKUP_DIR||DEFAULT_BACKUP_DIR;return adminJson(await githubListDir(env,dir))}
async function update(request,env){const gate=await requireAdmin(request,env);if(gate.response)return gate.response;const body=await request.json();const errors=validatePayload(body);if(errors.length)return adminJson({ok:false,error:"validation_failed",errors},400);const configPath=env.GITHUB_CONFIG_PATH||DEFAULT_CONFIG_PATH;const backupDir=env.GITHUB_BACKUP_DIR||DEFAULT_BACKUP_DIR;const cur=await githubGetFile(env,configPath);if(!cur.ok)return adminJson({ok:false,error:"github_read_failed",current:cur},502);const time=stamp();const backupPath=`${backupDir}/radio-runtime.${time}.back.json`;const latestPath=`${backupDir}/radio-runtime.latest.back.json`;const b1=await githubPutFile(env,backupPath,cur.content,`backup: ${configPath} ${time}`);if(!b1.ok)return adminJson({ok:false,error:"backup_failed",backup:b1},502);const latest=await githubGetFile(env,latestPath);const b2=await githubPutFile(env,latestPath,cur.content,"backup: update latest radio runtime backup",latest.ok?latest.sha:undefined);if(!b2.ok)return adminJson({ok:false,error:"latest_backup_failed",backup:b2},502);let old={};try{old=JSON.parse(cur.content)}catch{}const next={...old,primaryStream:String(body.primaryStream||old.primaryStream||"").trim(),backupStream:String(body.backupStream||old.backupStream||"").trim(),emergencyStream:String(body.emergencyStream||old.emergencyStream||"").trim(),version:Number(old.version||0)+1,updatedAt:new Date().toISOString(),updatedBy:"radio-player-admin-overlay",updateNote:String(body.note||"Admin overlay stream config update"),previousBackup:backupPath,latestBackup:latestPath};const put=await githubPutFile(env,configPath,JSON.stringify(next,null,2)+"\n",`admin: update radio runtime config v${next.version}`,cur.sha);if(!put.ok)return adminJson({ok:false,error:"config_update_failed",put},502);return adminJson({ok:true,configPath,backupPath,latestBackupPath:latestPath,version:next.version,commit:put.data?.commit?.sha||null,note:"GitHub commit created. Cloudflare auto-deploy should start automatically."})}
async function rollback(request,env){const gate=await requireAdmin(request,env);if(gate.response)return gate.response;const configPath=env.GITHUB_CONFIG_PATH||DEFAULT_CONFIG_PATH;const backupDir=env.GITHUB_BACKUP_DIR||DEFAULT_BACKUP_DIR;const latestPath=`${backupDir}/radio-runtime.latest.back.json`;const cur=await githubGetFile(env,configPath);if(!cur.ok)return adminJson({ok:false,error:"current_read_failed",current:cur},502);const latest=await githubGetFile(env,latestPath);if(!latest.ok)return adminJson({ok:false,error:"latest_backup_read_failed",latest},502);const restore=await githubPutFile(env,configPath,latest.content,`rollback: restore ${configPath} from latest backup`,cur.sha);if(!restore.ok)return adminJson({ok:false,error:"rollback_failed",restore},502);return adminJson({ok:true,restoredFrom:latestPath,configPath,commit:restore.data?.commit?.sha||null})}
export async function handleRadioAdminConfigAddon(request,env){const url=new URL(request.url);if(url.pathname==="/api/admin/debug")return adminJson({ok:true,service:"radio-admin-config-addon",hasGithubToken:!!env.GITHUB_TOKEN,githubOwner:env.GITHUB_OWNER||null,githubRepo:env.GITHUB_REPO||null,githubBranch:env.GITHUB_BRANCH||null,configPath:env.GITHUB_CONFIG_PATH||DEFAULT_CONFIG_PATH,backupDir:env.GITHUB_BACKUP_DIR||DEFAULT_BACKUP_DIR,authVerifyUrl:env.ADMIN_AUTH_VERIFY_URL||"https://666-system-auth.666soundsdesign-broadcaster.com/verify"});if(url.pathname==="/api/admin/auth-check"){const auth=await verifyAdminAuth(request,env);return adminJson({ok:!!auth.ok,auth},auth.ok?200:401)}if(url.pathname==="/api/admin/config/current"&&request.method==="GET")return current(request,env);if(url.pathname==="/api/admin/config/backups"&&request.method==="GET")return backups(request,env);if(url.pathname==="/api/admin/config/update"&&request.method==="POST")return update(request,env);if(url.pathname==="/api/admin/config/rollback"&&request.method==="POST")return rollback(request,env);return null}
